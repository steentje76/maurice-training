const fs=require('fs');const vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');function ok(x,m){if(!x)throw Error(m)}ok(!h.includes('}\\nfunction'),'literal \\n between JS declarations freezes browser parsing');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);for(const [i,s] of scripts.entries()){try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}}console.log('Design Engine browser parse regression: PASS');