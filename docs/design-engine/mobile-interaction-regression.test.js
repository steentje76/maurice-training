const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');function ok(x,m){if(!x)throw Error(m)}ok(/\.panelBackdrop\{display:none;[^}]*pointer-events:none/.test(h),'hidden backdrop must be display:none and pointer-events:none');ok(/\.panelBackdrop\.on\{display:block;pointer-events:none\}/.test(h),'mobile backdrop must never intercept taps');\nok(h.includes("safeRenderPart('workspace',renderWorkspace)")&&h.indexOf("screen.innerHTML=renderMarkup")<h.indexOf("safeRenderPart('workspace'"),'core phone render must happen before optional inspectors');ok(!h.includes("}}\\\\nfunction render(){"),'literal backslash-n before core render is forbidden');\nconst scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('Mobile interaction regression gate: PASS');
