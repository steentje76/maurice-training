const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');require('./test-version').requireUxLabVersion(h,2,36);function ok(x,m){if(!x)throw Error(m)}ok(!h.includes("}\\\\nfunction showProjectHome"),'project launcher must not contain literal backslash-n between functions');ok(!h.includes("}\\\\n</style>"),'style must not contain literal backslash-n before close');ok(h.includes('id="projectHome"')&&h.includes('Kies een project'),'project home required');ok(h.includes("openDesignProject('trainingskompas')"),'TK pack must be launchable');['MedScan','Radioplanner','Reisplanner'].forEach(x=>ok(h.includes('<b>'+x+'</b>'),'planned project '+x));ok(h.includes("if(id!=='trainingskompas')return"),'unavailable packs must not activate');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('Design Engine v2.36 project home: PASS');
