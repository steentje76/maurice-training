const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');require('./test-version').requireUxLabVersion(h,2,33);function ok(x,m){if(!x)throw Error(m)}for(const x of ['function proposalLabel','function proposalClass',"proposalLabel('my-training','Mijn trainingen')","proposalClass('my-training')",'proposalLabel(x[0],x[1])','proposalClass(x[0])','proposal-compact','proposal-emphasis'])ok(h.includes(x),x+' missing');ok(h.includes("if(composerMode!=='proposal')return fallback"),'labels must be proposal-only');ok(h.includes("if(composerMode!=='proposal')return ''"),'variants must be proposal-only');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('Design Engine v2.33 proposal application: PASS');