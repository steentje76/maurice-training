const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');const C=require('./contract-exporter');require('./test-version').requireUxLabVersion(h,2,34);function ok(x,m){if(!x)throw Error(m)}ok(h.includes('composerMode!=="proposal"'),'freeze must require Proposal mode');ok(h.includes('proposal:{mode:composerMode'),'UI must pass proposal snapshot');const flow={id:'FLOW-X',goal:'x',invariants:[],nodes:[],edges:[]},scenario={id:'A',name:'A',capability_coverage:[],required_states:[],designed_states:[],data_bindings:[],components:[],accessibility:{}},validation={freeze_ready:true,applicable_rule_ids:[],engine_version:'x',hard_issue_count:0,hard_conflict_count:0,route_pass:true,evidence_pass:true,capability_pass:true,state_pass:true,a11y_pass:true,source_pass:true,coverage_pass:true},snap={verified_against_main:'abc',sources:{}};const proposal={mode:'proposal',overrides:{strength:{label:'Power',variant:'emphasis'}}};const freeze=C.createFreeze({flow,scenario,validation,sourceSnapshot:snap,selectedBy:'PO',approvedAt:'2026-09-19T00:00:00Z',proposal});ok(freeze.proposal.overrides.strength.label==='Power','freeze proposal snapshot');proposal.overrides.strength.label='mutated';ok(freeze.proposal.overrides.strength.label==='Power','freeze must clone proposal');const contract=C.createContract({freeze,flow,scenario,repository:'repo'});ok(contract.proposal.overrides.strength.variant==='emphasis','contract must carry frozen proposal');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('Design Engine v2.34 proposal freeze chain: PASS');