const assert=require('assert'),S=require('./screen-composer'),V=require('./composition-validation'),R=require('./design-repository'),C=require('./contract-exporter'),P=require('./proposal-pipeline');
(async()=>{const pack={id:'GENERIC',components:{components:[{id:'B',type:'button',interactive:true}]},screens:{screens:[{id:'HOME',instances:[{id:'go',component:'B',label:'Go',primary_cta:true,rule_ids:['UX-CTA-001']}]}]}};
const repo=R.memoryRepository(),p=P.createProposalPipeline({composer:S.createScreenComposer(pack),validator:V.createCompositionValidator({rules:[]}),repository:repo,exporter:C});
let proposal=await p.propose({proposalId:'P1',screenId:'HOME',viewport:390,changes:{label:'Candidate'}});assert.equal(proposal.status,'PROPOSED');assert.equal(proposal.validation.valid,true);assert.rejects(()=>p.freeze({proposalId:'P1'}),/PROPOSAL_APPROVAL_REQUIRED/);
proposal=await p.review('P1','APPROVED','Product Owner');assert.equal(proposal.status,'APPROVED');
const flow={id:'FLOW-G',goal:'generic',invariants:[],nodes:[],edges:[]},scenario={id:'S1',name:'Generic',capability_coverage:[],required_states:[],designed_states:[],data_bindings:[],components:[],accessibility:{}},snap={verified_against_main:'abc',sources:{}};
const bundle=await p.freeze({proposalId:'P1',flow,scenario,sources:{},sourceSnapshot:snap,selectedBy:'Product Owner',approvedAt:'2026-09-19T18:00:00+02:00',repositoryName:'example/repo'});assert.equal(bundle.freeze.status,'DESIGN_FROZEN');assert.equal(bundle.freeze.proposal.id,'P1');assert.equal(bundle.contract.status,'BUILDABLE_FROM_FROZEN_DESIGN');
await p.propose({proposalId:'P2',screenId:'HOME'});assert.rejects(()=>p.review('P2','YES','PO'),/Invalid review decision/);console.log('V1 PROPOSAL REVIEW FREEZE PIPELINE PASS')})().catch(e=>{console.error(e);process.exit(1)});
