const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');require('./test-version').requireUxLabVersion(h,2,32);function ok(x,m){if(!x)throw Error(m)}for(const x of ['id="runtimeHealth"','Design Pack laden…','Design Pack gereed','Design Pack kon niet laden','window.designRuntimeError=e'])ok(h.includes(x),x+' missing');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('Design Engine v2.32 runtime health: PASS');