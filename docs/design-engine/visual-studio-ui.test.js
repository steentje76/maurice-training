const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');for(const x of ['Visual Studio','id="mockupUpload"','function loadMockupReference','function moveVisualTile','function setVisualToken','function renderVisualStudio','visualOrder()','visualStyleAttr(id)','./visual-studio.js'])if(!h.includes(x))throw Error(x+' missing');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);scripts.forEach((s,i)=>{try{new vm.Script(s)}catch(e){throw Error('inline script '+i+' parse failure: '+e.message)}});console.log('VISUAL STUDIO UI GATE PASS');