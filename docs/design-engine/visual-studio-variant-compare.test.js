const assert=require('assert'),{createVisualStudio}=require('./visual-studio');const v=createVisualStudio({order:['a','b']});v.selectVariant('B');v.move('b',0);v.patchStyle('a',{radius:9});let d=v.variantDiff('A','B');assert.strictEqual(d.orderChanged,true);assert.deepStrictEqual(d.styleScopes,['a']);assert.strictEqual(d.changeCount,2);v.selectProposalVariant('B');assert.strictEqual(v.snapshot().selectedVariant,'B');assert.throws(()=>v.selectProposalVariant('D'),/UNKNOWN_VARIANT/);console.log('VISUAL STUDIO VARIANT COMPARE SELECT PASS');