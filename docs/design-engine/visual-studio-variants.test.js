const assert=require('assert'),{createVisualStudio}=require('./visual-studio');const v=createVisualStudio({order:['a','b','c']});v.move('c',0);v.patchStyle('a',{radius:7});assert.deepStrictEqual(v.snapshot().order,['c','a','b']);v.selectVariant('B');assert.deepStrictEqual(v.snapshot().order,['a','b','c']);assert.strictEqual(v.snapshot().styles.a,undefined);v.cloneVariant('A','B');assert.deepStrictEqual(v.snapshot().order,['c','a','b']);assert.strictEqual(v.snapshot().styles.a.radius,7);v.patchStyle('a',{radius:22});assert.strictEqual(v.snapshot().styles.a.radius,22);v.selectVariant('A');assert.strictEqual(v.snapshot().styles.a.radius,7);assert.throws(()=>v.selectVariant('D'),/UNKNOWN_VARIANT/);console.log('VISUAL STUDIO ABC VARIANTS PASS');