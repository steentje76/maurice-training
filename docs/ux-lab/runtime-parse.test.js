const fs=require('fs'),vm=require('vm');const h=fs.readFileSync(__dirname+'/index.html','utf8');const scripts=[...h.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m=>m[1]).filter(Boolean);if(!scripts.length)throw Error('no inline scripts found');scripts.forEach((s,i)=>{try{new vm.Script(s,{filename:'index-inline-'+i+'.js'})}catch(e){throw Error('INLINE SCRIPT '+i+' PARSE FAILURE: '+e.message)}});console.log('Full inline JavaScript parse gate: PASS ('+scripts.length+' scripts)');
