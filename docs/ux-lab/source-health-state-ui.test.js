const fs=require('fs');const h=fs.readFileSync(__dirname+'/index.html','utf8');function ok(x,m){if(!x)throw Error(m)}for(const x of ['id="sourceHealth"','id="stateExplorer"','function renderSourceHealth','function renderStates','REGISTRY ↔ SNAPSHOT MATCH','REGISTRY ↔ SNAPSHOT DRIFT','HISTORICAL BASELINE METADATA DRIFT','Actual blob ↔ pin: CI GATE','required_states','designed_states'])ok(h.includes(x),x+' missing');ok(h.includes('snap===s.sha'),'source SHA comparison missing');ok(h.includes('verified_against_main===window.tkSources.baseline_sha'),'historical baseline metadata comparison missing');ok(!h.includes("?'PIN MATCH':'DRIFT'"),'legacy ambiguous source status remains');console.log('UX Lab source health/state gate: PASS');